# CI-only dummy value so the chart renders (mysql.password is required; terraform
# supplies the real value at deploy time). Used by the helm-validate workflow.
mysql:
  password: ci-dummy-mysql-password
