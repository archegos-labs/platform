apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metadata-mysql
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.registry.db.storage }}
